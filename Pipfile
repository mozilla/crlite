[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
bsdiff4 = ">=1.1"
cryptography = ">=2.2"
glog = ">=0.3"
kinto-http = ">=10.9"
python-decouple = ">=3.1"
requests = {extras = ["socks"],version = ">=2.10.0"}

[requires]
python_version = "3.7"
